/*
 * File: PlayerType.cs
 *
 * Purpose:
 * Describes how a player slot should behave in the AI prototype scene.
 * Existing local GameScene logic can ignore this enum.
 */

public enum PlayerType
{
    Human,
    AI,
    Empty
}
